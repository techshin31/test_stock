"""적극투자형 투자성향 설정

집중 투자 + 빠른 진입으로 수익을 극대화하는 성향.
ADX 임계값을 낮춰 더 많은 추세에 진입하고 포지션 비중을 높게 유지한다.
"""

from .base import FEES, SLIPPAGE  # noqa: F401

# ── ADX 기본값 ────────────────────────────────────────────────────────────────
ADX_THRESHOLD = 20.0   # 낮은 임계값 → 더 많은 추세 진입 허용
ADX_SIDEWAYS  = 15.0   # 횡보 판별 기준 (엄격하게)

# ── 분할 매수/매도 비중 ───────────────────────────────────────────────────────
ENTRY1_SIZE      = 0.6   # 1차 매수: 골든크로스 (더 공격적 초기 진입)
ENTRY2_SIZE      = 0.9   # 2차 매수: MA20 지지 확인 (고비중 유지)
ENTRY_RANGE_SIZE = 0.4   # 횡보 매수: BB 하단
EXIT1_SIZE       = 0.5   # TRANSITION 전환 후 유지 비중 (절반 유지)
EXIT2_SIZE       = 0.2   # 데드크로스 후 유지 비중
RECENT_WINDOW    = 40    # 2차 매수 유효 기간 (거래일, 더 짧게)

# ── 포트폴리오 모멘텀 윈도우 ──────────────────────────────────────────────────
MOMENTUM_WINDOW = {
    "UPTREND":    126,
    "TRANSITION":  63,
    "SIDEWAYS":    21,
}
MIN_MOMENTUM = 0.05   # 모멘텀 최소 기준 상향 (양의 모멘텀 종목만 진입)
