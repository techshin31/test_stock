"""위험중립형 투자성향 설정

분산 투자 + 단계적 진입/청산으로 손실을 제한하는 성향.
ADX 임계값을 높게 유지해 강한 추세에서만 진입한다.
"""

from .base import FEES, SLIPPAGE  # noqa: F401

# ── ADX 기본값 ────────────────────────────────────────────────────────────────
ADX_THRESHOLD = 25.0   # 추세 진입 강도 기준 (높을수록 강한 추세에서만 진입)
ADX_SIDEWAYS  = 20.0   # 횡보 판별 기준

# ── 분할 매수/매도 비중 ───────────────────────────────────────────────────────
ENTRY1_SIZE      = 0.4   # 1차 매수: 골든크로스
ENTRY2_SIZE      = 0.7   # 2차 매수: MA20 지지 확인 (누적 목표 비중)
ENTRY_RANGE_SIZE = 0.3   # 횡보 매수: BB 하단
EXIT1_SIZE       = 0.4   # TRANSITION 전환 후 유지 비중
EXIT2_SIZE       = 0.1   # 데드크로스 후 유지 비중
RECENT_WINDOW    = 60    # 2차 매수 유효 기간 (거래일)

# ── 포트폴리오 모멘텀 윈도우 ──────────────────────────────────────────────────
MOMENTUM_WINDOW = {
    "UPTREND":    126,   # 추세 지속 구간 → 장기 모멘텀
    "TRANSITION":  63,   # 방향 불확실 → 중기
    "SIDEWAYS":    21,   # 단기 등락 반복 → 단기 반응
}
MIN_MOMENTUM = 0.0

# ── ATR Stop-Loss ─────────────────────────────────────────────────────────────
ATR_PERIOD     = 14    # ATR 계산 기간
ATR_MULTIPLIER = 2.0   # stop-loss = 전일 ATR × 2 (노이즈 범위 초과 시 청산)

# ── KOSPI 시장 필터 ───────────────────────────────────────────────────────────
KOSPI_MA = 120         # KOSPI 가 MA120 아래면 신규 진입 금지

# ── 현금 수익 (실전: KODEX 단기채권 ETF 153130.KS) ────────────────────────────
CASH_RETURN = 0.035    # 연수익률 3.5% — 백테스팅 시 현금 보유 기간 수익 보정용
