"""
백테스팅 전략 파라미터 설정

Walk-Forward 최적화 탐색 범위와 기본값을 한 곳에서 관리한다.
노트북에서 직접 하드코딩하지 않고 이 파일을 import해서 사용한다.
"""

# ── 거래 비용 ────────────────────────────────────────────────────────────────
FEES = 0.0015       # 수수료 0.15% (증권사 기본)
SLIPPAGE = 0.001    # 슬리피지 0.1%

# ── MA 이동평균 윈도우 ────────────────────────────────────────────────────────
MA_WINDOWS = (20, 60, 120)   # 단기 / 중기 / 장기
ADX_WINDOW = 14

# ── ADX 파라미터 기본값 ───────────────────────────────────────────────────────
# Walk-Forward 탐색 후 선택된 최적값이 없을 때 사용하는 초기값
ADX_THRESHOLD_DEFAULT = 25.0   # 추세 진입 강도 기준
ADX_SIDEWAYS_DEFAULT  = 20.0   # 횡보 판별 기준

# ── Walk-Forward 탐색 범위 (위험중립형) ──────────────────────────────────────
# adx_threshold: 추세 진입 강도. 높을수록 강한 추세에서만 진입 (휩소 감소, 신호 감소)
# adx_sideways : 횡보 판별 기준. 낮을수록 횡보 판별이 엄격해짐 (볼린저밴드 적용 범위 축소)
ADX_PARAM_GRID = {
    "adx_threshold": [15, 20, 25, 30],   # 4가지
    "adx_sideways":  [10, 15, 20],        # 3가지 → 총 12조합
}

# ── Walk-Forward 학습/검증 구간 ───────────────────────────────────────────────
WF_TRAIN_MONTHS = 12   # 학습 구간 (개월)
WF_TEST_MONTHS  = 6    # 검증/적용 구간 (개월)

# ── 볼린저 밴드 ──────────────────────────────────────────────────────────────
BB_WINDOW = 20
BB_STD    = 2.0

# ── 분할 매수/매도 비중 (위험중립형) ──────────────────────────────────────────
ENTRY1_SIZE       = 0.4   # 1차 매수: 골든크로스
ENTRY2_SIZE       = 0.7   # 2차 매수: MA20 지지 확인 (누적 목표 비중)
ENTRY_RANGE_SIZE  = 0.3   # 횡보 매수: BB 하단
EXIT1_SIZE        = 0.4   # TRANSITION 전환 후 유지 비중
EXIT2_SIZE        = 0.1   # 데드크로스 후 유지 비중
RECENT_WINDOW     = 60    # 2차 매수 유효 기간 (거래일)

# ── 포트폴리오 모멘텀 윈도우 ──────────────────────────────────────────────────
MOMENTUM_WINDOW = {
    "UPTREND":    126,   # 추세 지속 구간 → 장기 모멘텀
    "TRANSITION":  63,   # 방향 불확실 → 중기
    "SIDEWAYS":    21,   # 단기 등락 반복 → 단기 반응
}
MIN_MOMENTUM = 0.0
